// This is a single line comment!
/* This is a multi
   line comment! */
fn id(a: i64) -> i64 {
  let test = 10;
  let s = "abc";
  let unary = -1;
  let binary = 1.0 - -3.0;
  let call = id(2);
  for (let i = 0; i < 10; i++) {
    let b = -4 * 4 < 7 - 2;
  }
  return a;
}

fn main() {
  let x = true;

  while (x) {
    idx(1);
  }

  if (x) {
    id(2);
  } else {
    id(4);
  }
}
