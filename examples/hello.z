// This is a single line comment!
/* This is a multi
   line comment! */
fn id(a: i64) -> i64 {
  return a;
}

fn main() -> i32 {
  let x = 1;
  printf("x: %d s: %s\n", x, "hello world");

  if (x > 0) {
    id(2);
  } else {
    id(4);
  }

  return 0;
}
