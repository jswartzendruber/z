// This is a single line comment!
/* This is a multi
   line comment! */
fn id(a: int) {
  return a;
}

fn main() {
  let x = true;

  while (x) {
    idx(1);
  }

  if (x) {
    id(2);
  } else {
    id(4);
  }
}
