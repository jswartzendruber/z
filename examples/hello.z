// This is a single line comment!
/* This is a multi
   line comment! */
fn id(a: int) -> int {
  let test: i64 = 10;
  for (let i = 0; i < 10; i++) {
    let b = -4 * 4 < 7 - 2;
  }
  return a;
}

fn main() {
  let x = true;

  while (x) {
    idx(1);
  }

  if (x) {
    id(2);
  } else {
    id(4);
  }
}
