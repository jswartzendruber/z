fn fib(n: i64) -> i64 {
    if (n == 0) {
        return 0;
    }
    if (n == 1) {
        return 1;
    }

    return fib(n - 1) + fib(n - 2);
}

fn main() -> i32 {
    let x: i64 = 35;
    printf("fib(%li) = %li\n", x, fib(x));
    return 0;
}